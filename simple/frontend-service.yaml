apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: guestbook
    tier: frontend
spec:
  type: NodePort
  externalName: external-frontend.default.svc.cluster.local
  ports:
  - port: 80
  selector:
    app: guestbook
    tier: frontend
